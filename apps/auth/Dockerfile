FROM svhd/logto:latest

ARG RAILWAY_ENVIRONMENT
ARG PORT
ARG DATABASE_URL

ENV RAILWAY_ENVIRONMENT=$RAILWAY_ENVIRONMENT
ENV PORT=$PORT
ENV DB_URL=$DATABASE_URL

CMD ["sh", "-c", "npm run cli db seed -- --swe && npm start"]